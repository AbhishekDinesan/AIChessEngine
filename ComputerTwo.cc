#include "AbstractComputer.h"

class ComputerTwo : public Computer
{
public:
    void makeMove(int startFile, int startRank, int endFile, int endRank)
        override
    {
        /*
        vector v = generateAllMoves();
        for (int i = 0; i < ; i++){
            //perform a deep copy of the board (either normal or string)
            // execute the move
            // see if it produces a check

        */
    }
};